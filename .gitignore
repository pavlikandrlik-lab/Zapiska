# OS / IDE
.DS_Store
.vs/
.vscode/
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates

# .NET build outputs
**/bin/
**/obj/
TestResults/
artifacts/
coverage/
*.coverage
*.trx

# Publish artifacts
publish/
publish.zip

# Local DB files
.mssql/
.mssql/data/
*.mdf
*.ldf

# Local tools cache
.tools/

# Logs / local overrides
*.log
*.local.json
gov-kits/

# Runtime credentials (real credentials nikdy v gitu — viz docs/technical/14-local-dev-secrets.md).
# appsettings.example.json zůstává v gitu jako schema template.
PmTracker.Web/appsettings.json
PmTracker.Web/appsettings.Development.json
PmTracker.Web/appsettings.Production.json

# Citlivé vzorky dokumentů (reálné výzvy, tickety apod.) — nesmí se commitovat
*.docx
*.doc
*.xlsx
*.xls
