namespace PmTracker.Web.TagHelpers;

/// <summary>Vizuální typ pro pm-badge (gov-tag).</summary>
public enum PmBadgeType
{
    /// <summary>Jemné pozadí (subtle) — výchozí.</summary>
    Subtle,
    /// <summary>Výrazné pozadí (bold).</summary>
    Bold
}
